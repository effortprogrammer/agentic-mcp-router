# mcp-tool-router (Python)

Python wrapper for the MCP tool router daemon. This package handles MCP
synchronization and LLM orchestration while delegating search and working-set
logic to the TypeScript core via JSON-RPC.
